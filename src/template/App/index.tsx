import type { NextPage } from 'next';

import * as Styled from './styles';

const Home: NextPage = () => {
  return <Styled.Container background='green'>Hello</Styled.Container>;
};

export default Home;
